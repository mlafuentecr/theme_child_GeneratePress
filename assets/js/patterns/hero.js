/*===============================================================================*/
/* START hero.js                                                                */
/*===============================================================================*/

document.addEventListener('DOMContentLoaded', () => {
  // Start execution once the DOM content is fully loaded
  async function startJs() {
    if (!document.querySelector('.signifi-hero--animated')) return;
    console.log('Signifi Hero JS loaded');
  }

  startJs();
});
