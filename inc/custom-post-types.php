<?php
/**
 * Custom Post Types
 *
 * Register project-specific custom post types here using register_post_type().
 * This file is NOT loaded by functions.php yet — add it to the $includes array
 * when post types are needed.
 *
 * Example:
 *   register_post_type( 'portfolio', [ 'public' => true, 'label' => 'Portfolio' ] );
 */

if ( ! defined( 'ABSPATH' ) ) {
    exit;
}
