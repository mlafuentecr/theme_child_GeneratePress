<?php
/**
 * Meta Boxes
 *
 * Register custom meta boxes here using add_meta_box() inside an 'add_meta_boxes' action.
 * This file is NOT loaded by functions.php yet — add it to the $includes array
 * when meta boxes are needed.
 *
 * Example:
 *   add_action( 'add_meta_boxes', function () {
 *       add_meta_box( 'my_box', 'My Box', 'my_box_cb', 'post' );
 *   } );
 */

if ( ! defined( 'ABSPATH' ) ) {
    exit;
}
